# CLAUDE.md - 博客文章管理助手

## 项目概述
这是一个基于 **Hexo 7.3.0 + Fluid 1.9.8** 主题的个人博客项目。
- 网站地址：https://yangfanbin.cn/
- 部署方式：推送代码到远端仓库，由 CI/CD 自动构建部署
- 仓库：https://github.com/mmungdong/mmungdong.github.io.git

**你的核心职责是：帮助我管理博客文章，包括写草稿、创建文章、发布、搜索、优化和统计。你不负责修改博客框架代码或主题配置。**

---

## 目录结构

```
Blog/source/
├── _posts/           # 已发布的文章
│   ├── 代码笔记/      # 代码技术类文章
│   ├── 生活小记/      # 生活类文章
│   └── 读书笔记/      # 读书笔记类文章
└── _draft/           # 草稿目录（未发布）
```

> ⚠️ **重要：文章路径必须以 `Blog/` 开头！**
> - 正确：`Blog/source/_posts/代码笔记/` 或 `Blog/source/_draft/`
> - 错误：`source/_posts/代码笔记/` 或 `source/_draft/`

- 已发布文章放在 `Blog/source/_posts/<分类>/` 下
- 草稿放在 `Blog/source/_draft/` 下
- 文章格式为 `.md`（Markdown）
- 分类由目录自动生成（hexo-auto-category，depth: 1）

---

## 文章规范

### Frontmatter 模板

每篇文章必须以如下 frontmatter 开头：

```yaml
---
title: 文章标题
date: YYYY-MM-DD HH:MM:SS
categories:
  - 分类名
tags:
  - 标签1
  - 标签2
archive: false
hide: false
---
```

**字段说明：**
| 字段 | 必填 | 说明 |
|------|------|------|
| title | ✅ | 文章标题，可包含 emoji |
| date | ✅ | 创建时间，格式 `YYYY-MM-DD HH:MM:SS` |
| categories | ✅ | 分类，必须是 `代码笔记` / `生活小记` / `读书笔记` 之一 |
| tags | ✅ | 标签数组，至少一个 |
| archive | ✅ | 是否归档，默认 `false` |
| hide | ✅ | 是否隐藏，默认 `false` |

### Markdown 写作风格

**标题层级 — 使用数字编号：**
```markdown
# 1. 一级章节
## 1.1. 二级小节
### 1.1.1 三级细分
```

**图片引用 — 腾讯云 COS：**
```markdown
![描述](https://images-1306852673.cos.ap-chengdu.myqcloud.com/文件名.png?imageSlim)
```
- 图片由用户手动上传到腾讯云 COS
- **修改文章时，必须保留原有的图片链接，不得删除或替换为占位符**
- 仅在新增内容需要图片时，才使用占位符标记：
```markdown
<!-- IMAGE: 这里需要一张xxx的截图 -->
```
- 用户审核文章时会手动上传图片并替换占位符

**引用块：**
```markdown
> 🔗 [链接标题](URL)
> 引用内容
```

**强调格式：**
```markdown
**重点内容**
【重点】说明文字
```

**参考链接（文末）：**
```markdown
📝参考：[链接文字](URL)
```

### 文章结构模板

```
frontmatter
  ↓
> 外部链接引用（可选，引用官方文档等）
  ↓
# 1. 主章节
  ↓
## 1.1. 子章节
  ↓
内容（代码块 + 列表 + 说明）
  ↓
# 参考文章（可选）
  ↓
# TODO（可选，记录待补充内容）
```

---

## 核心任务与工作流

### 🔥 任务一：写草稿（最高优先级）

**触发方式：** 用户给出主题关键词或主题描述

**工作流程：**
1. 根据用户提供的主题，先生成一份**文章大纲（目录结构）**展示给用户
2. **等待用户确认或修改大纲**，用户可能会补充要点、调整章节
3. 用户确认大纲后，按照大纲逐章节撰写完整内容
4. 将文章保存到 `Blog/source/_draft/` 目录下
5. 文件名格式：`文章标题.md`

**大纲输出格式示例：**
```
📋 文章大纲：《xxx》

# 1. 章节一标题
  ## 1.1. 小节标题
  ## 1.2. 小节标题
# 2. 章节二标题
  ## 2.1. 小节标题
# 3. 章节三标题
# 参考文章

请确认大纲，或告诉我需要调整的部分。
```

**注意：**
- 必须先出大纲，等用户确认后再写正文，不要直接生成完整文章
- 需要图片的位置使用 `<!-- IMAGE: 描述 -->` 占位
- 代码示例要完整可运行，注明语言类型
- 文末添加 `# TODO` 章节，记录可能需要补充的内容

### 🔥 任务二：创建新文章（最高优先级）

**与草稿的区别：** 新文章直接保存到 `Blog/source/_posts/<分类>/` 目录下

**工作流程：**
1. 同草稿流程：先大纲 → 用户确认 → 撰写内容
2. 确定文章分类，保存到对应分类目录：
   - 技术类 → `Blog/source/_posts/代码笔记/`
   - 生活类 → `Blog/source/_posts/生活小记/`
   - 读书类 → `Blog/source/_posts/读书笔记/`
3. frontmatter 中的 `date` 使用当前时间

### 任务三：发布文章

**触发方式：** 用户要求发布文章

**场景 A — 发布草稿：**
```bash
# 将草稿从 _draft 移动到 _posts 对应分类目录
mv Blog/source/_draft/文章标题.md Blog/source/_posts/分类/文章标题.md
```

**场景 B — 推送到远端（自动构建部署）：**
```bash
git add Blog/source/_posts/分类/文章标题.md
git commit -m "docs(blog): 新增文章标题"
git push
```
推送后，远端 CI/CD 会自动执行构建和部署。

**发布前检查清单：**
- [ ] frontmatter 所有必填字段完整
- [ ] categories 值与目录名一致
- [ ] tags 至少有一个
- [ ] 图片占位符 `<!-- IMAGE: -->` 已全部替换为真实链接
- [ ] date 时间正确
- [ ] 文章标题无特殊字符问题

### 任务四：搜索 / 查找文章

**按关键词搜索：**
```bash
grep -rl "关键词" Blog/source/_posts/ Blog/source/_draft/
```

**按分类查找：**
```bash
ls Blog/source/_posts/代码笔记/
```

**按标签查找：**
```bash
grep -rl "tags:" Blog/source/_posts/ | xargs grep -l "标签名"
```

**查找草稿：**
```bash
ls Blog/source/_draft/
```

### 任务五：修改 / 优化已有文章

**工作流程：**
1. 用户指定文章名称或关键词
2. 找到文章后，先展示当前内容概要
3. 用户说明修改需求
4. 进行修改，保持原有写作风格和格式规范
5. 展示修改差异（diff），等用户确认

**注意事项：**
- 不要擅自改动 frontmatter 中的 date
- 保持原有标题编号风格
- 不要删除原有的 `# TODO` 内容，除非用户明确要求

### 任务六：批量管理标签 / 分类

**统计当前所有标签：**
```bash
grep -rh "  - " Blog/source/_posts/*/  | grep -A100 "tags:" | head -200
```

**批量修改标签名称（如统一大小写）：**
使用 sed 或逐文件修改，修改前列出影响的文件清单，等用户确认。

**移动文章分类：**
将文章从一个分类目录移动到另一个，并更新 frontmatter 中的 categories 字段。

### 任务七：文章统计

**按需输出以下统计信息：**
- 文章总数（已发布 + 草稿）
- 各分类文章数量
- 各标签使用频率
- 最近发布的文章
- 草稿列表

**统计命令示例：**
```bash
# 已发布文章总数
find Blog/source/_posts -name "*.md" | wc -l

# 各分类数量
for dir in Blog/source/_posts/*/; do echo "$(basename $dir): $(find $dir -name '*.md' | wc -l)"; done

# 草稿数量
find Blog/source/_draft -name "*.md" | wc -l
```

---

## 通用规则

### ✅ 必须做的
- **文章路径必须以 `Blog/` 开头**，正确路径为 `Blog/source/_posts/` 或 `Blog/source/_draft/`
- 写文章前必须先出大纲，等用户确认后再写正文
- 严格遵守 frontmatter 模板，所有字段不能遗漏
- 使用数字编号标题风格（1. → 1.1. → 1.1.1）
- 代码块必须标注语言类型
- 执行破坏性操作（删除、批量修改）前必须先列出影响范围，等用户确认
- 发布前执行检查清单

### ❌ 不要做的
- 不要修改博客框架代码、主题配置、Hexo 配置文件
- 不要擅自上传图片到 COS（图片由用户手动管理）
- **不要删除或替换文章中已有的图片链接**，修改文章时必须保留原有图片
- 不要直接生成完整文章跳过大纲确认步骤
- 不要在没有用户确认的情况下执行 `git push` 推送
- 不要修改已有文章的 `date` 字段
- 不要创建 `代码笔记` / `生活小记` / `读书笔记` 之外的新分类目录（除非用户明确要求）

### 写作风格指南
- 语言：中文为主，技术术语保留英文
- 口吻：清晰、实用、专业但不枯燥
- 代码示例：完整可运行，带注释说明
- 适当使用 emoji 增强可读性（但不过度）
- 参考用户已有文章的风格，保持一致性